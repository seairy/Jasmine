<div class="content">
  <div class="panel panel-notice">
    <h1>确认购买成功</h1>
    <% unless @task.final_paid? %>
    <p>等待买家支付尾款，之后您就可以发货了</p>
    <% end %>
    <%= link_to '返回', [:wechat, @task], class: 'btn btn-success' %>
  </div>
</div>
<%= footer_tag %>